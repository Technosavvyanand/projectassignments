import Link from 'next/link'
import { ArrowRight, FileText, Globe2, MessageCircle } from 'lucide-react'
import { CTA, Footer, Navbar, PageHero, SectionHeading } from '@/components/site'
export const metadata = { title: 'Resources', description: 'Practical ideas for clearer project work.' }
const resources = [{ icon: <FileText />, title: 'The one-page project brief', text: 'A simple structure for getting a team aligned before the work starts.' }, { icon: <Globe2 />, title: 'Choosing tools with intention', text: 'A practical lens for making technology decisions that last.' }, { icon: <MessageCircle />, title: 'Better questions, better work', text: 'How a sharper conversation can save weeks of unfocused effort.' }]
export default function ResourcesPage() { return <><Navbar /><main><PageHero eyebrow="Resources" title="Useful thinking for technical research." body="Practical notes for navigating cybersecurity, programming, research methods, technical writing, and postgraduate study with confidence." /><section className="page-content"><div className="container"><SectionHeading eyebrow="From the research desk" title="Ideas worth taking into your next submission." /><div className="service-detail-grid">{resources.map((resource) => <article className="service-detail" key={resource.title}><div className="icon-box">{resource.icon}</div><h3>{resource.title}</h3><p>{resource.text}</p><Link href="/contact" className="text-link">Talk it through <ArrowRight size={15} /></Link></article>)}</div></div></section><CTA /></main><Footer /></> }
